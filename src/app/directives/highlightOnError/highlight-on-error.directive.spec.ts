import { HighlightOnErrorDirective } from './highlight-on-error.directive';

describe('HighlightOnErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightOnErrorDirective();
    expect(directive).toBeTruthy();
  });
});
